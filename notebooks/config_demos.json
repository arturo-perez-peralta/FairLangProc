{
  "LOCAL": true,
  "ROOT_PATH_RELATIVE": "..",
  "metrics_config": {
    "embedding_model": "bert-base-uncased",
    "generation_model": "gpt2",
    "hurt_lexicon": [
      "fool", "bitch", "stupid", "incompetent", "idiot", "dumb"
    ]
  },
  "processors_config": {
    "base_model": "bert-base-uncased",
    "dataset": "imdb",
    "training_args": {
      "output_dir": "./results",
      "evaluation_strategy": "epoch",
      "per_device_train_batch_size": 8,
      "per_device_eval_batch_size": 8,
      "num_train_epochs": 1,
      "weight_decay": 0.01,
      "learning_rate": 2e-5
    },
    "eat_beta": 1.5,
    "embedding_lambda": 0.5
  }
}